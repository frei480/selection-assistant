# Selection Assistant - Автономное приложение с интеграцией LM Studio

Electron приложение для выделения текста с интеграцией AI через LM Studio. Адаптировано из компонента SelectionService проекта Cherry Studio.

## 📋 Возможности

- **Обнаружение выделенного текста** - глобальный мониторинг выделения текста в любых приложениях (Windows/macOS)
- **Всплывающая панель инструментов** - контекстная панель с кнопками действий
- **Интеграция с LM Studio** - локальный AI сервер для обработки текста
- **Несколько режимов триггера:**
  - Selection - при выделении текста
  - Ctrlkey - при нажатии определенной клавиши
  - Shortcut - при нажатии горячей клавиши
- **Окно результатов** - отображение результатов обработки
- **Персистентные настройки** - сохранение конфигурации

## 🚀 Установка

### Требования
- Node.js 18+
- npm или yarn
- LM Studio (для функциональности AI)

### Шаги установки

```bash
# Клонировать репозиторий
git clone [repository-url]
cd selection-assistant

# Установить зависимости
npm install

# Запустить в режиме разработки
npm run dev
```

## 🔧 Конфигурация LM Studio

1. **Установите LM Studio** - скачайте с [https://lmstudio.ai/](https://lmstudio.ai/)

2. **Загрузите модель** - выберите и загрузите модель в LM Studio

3. **Запустите локальный сервер** - в LM Studio нажмите "Start Server"

4. **Настройте приложение:**
   - Откройте Settings (Параметры)
   - Перейти в "LM Studio Integration"
   - Заполните параметры подключения:
     - Host: `localhost`
     - Port: `1234` (по умолчанию)
     - API Path: `/v1`
     - Model: выберите модель из списка

5. **Протестируйте соединение** - нажмите "Test Connection"

## 📝 Настройки

### Основные параметры

| Параметр | Описание | По умолчанию |
|----------|---------|-------------|
| Trigger Mode | Способ активации (selection/ctrlkey/shortcut) | selection |
| Follow Toolbar | Позиционировать окно результата рядом с панелью | true |
| Remember Window Size | Запомнить размер окна результатов | false |
| Compact Mode | Компактный режим интерфейса | false |
| Application Filter Mode | Фильтрация приложений (default/whitelist/blacklist) | default |

### LM Studio параметры

| Параметр | Описание | По умолчанию |
|----------|---------|-------------|
| Host | Хост сервера LM Studio | localhost |
| Port | Порт сервера | 1234 |
| API Path | Путь API | /v1 |
| Model | Название модели | - |

## 🏗️ Структура проекта

```
src/
├── main/
│   ├── index.ts              # Точка входа приложения
│   ├── preload.ts            # IPC мост
│   ├── utils.ts              # Утилиты
│   └── services/
│       ├── ConfigManager.ts  # Управление настройками
│       ├── Logger.ts         # Логирование
│       ├── LMStudioService.ts # AI интеграция
│       └── SelectionService.ts # Обнаружение текста
├── renderer/
│   ├── App.tsx               # Главный компонент
│   ├── selection-toolbar.tsx  # Всплывающая панель
│   ├── result-window.tsx     # Окно результатов
│   ├── pages/
│   │   ├── MainPage.tsx
│   │   ├── SettingsPage.tsx
│   │   └── ResultPage.tsx
│   └── components/
│       ├── SelectionWidget.tsx
│       └── ResultWindow.tsx
└── shared/
    ├── types.ts              # TypeScript интерфейсы
    └── ipcChannels.ts        # IPC каналы

```

## 🛠️ Команды разработки

```bash
# Запустить в режиме разработки
npm run dev

# Тип-чекинг
npm run type-check

# Собрать для продакшена
npm run build

# Запустить собранное приложение
npm start
```

## 🔌 Интеграция с LM Studio

### API Endpoints

- **Тест подключения**: `GET http://localhost:1234/v1/models`
- **Генерация текста**: `POST http://localhost:1234/v1/completions`

### Примеры использования

```typescript
// Тестирование соединения
const response = await fetch('http://localhost:1234/v1/models')
const models = await response.json()

// Генерация ответа
const completion = await fetch('http://localhost:1234/v1/completions', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    model: 'model-name',
    prompt: 'Your prompt here',
    max_tokens: 512
  })
})
```

## 🎯 Реализованные действия

- **Copy** - копировать в буфер обмена
- **Explain** - объяснить текст с помощью AI
- **Summarize** - кратко суммаризировать
- **Translate** - перевести текст

## 📦 Зависимости

- **electron** - фреймворк для Electron приложений
- **react** - UI библиотека
- **electron-store** - управление настройками
- **selection-hook** - глобальное обнаружение выделения текста
- **antd** - компоненты интерфейса
- **styled-components** - CSS-in-JS стили

## ⚠️ Ограничения

- Работает только на **Windows** и **macOS** (требует доступ к Accessibility API)
- Требует разрешения на использование функций Accessibility на macOS
- Требует локального запущенного сервера LM Studio

## 🔐 Разрешения (macOS)

На macOS может потребоваться предоставить разрешение на доступ к Accessibility:
1. System Preferences → Security & Privacy → Accessibility
2. Добавьте Selection Assistant в список разрешенных приложений

## 📝 Лицензия

ISC

## 🤝 Благодарности

Логика адаптирована из проекта [Cherry Studio](https://github.com/CherryHQ/cherry-studio):
- SelectionService
- SelectionToolbar
- ShortcutService
- TrayService

## 📚 Дополнительная информация

- [LM Studio документация](https://lmstudio.ai/)
- [Electron документация](https://www.electronjs.org/docs)
- [Selection Hook GitHub](https://github.com/GyPapi/selection-hook)
